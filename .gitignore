# Private / local files — kept OUT of the public site

# industrial résumé (private)
020726_terray.pdf

# CV — currently unlinked; delete this line to publish it
cv.pdf

# full-resolution photo originals (web-optimized versions are what the site uses)
profile_original.jpg
figures/gallery/ts0_original.jpeg

# original figure sources (TIFF) — the .png versions are what the site uses
*.tif
*.tiff

# local backup of the site
backup.html

# local editor/tooling config
.claude/
.DS_Store

# notes for Claude Code (local only)
CLAUDE.md

# personal editing guide — keep local, not on the public repo
README.md
